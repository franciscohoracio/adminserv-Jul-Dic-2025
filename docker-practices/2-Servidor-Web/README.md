# Práctica 2: Arrancar un servidor web y exponer el puerto

En esta práctica, vamos a arrancar un servidor web Nginx en un contenedor y exponer el puerto 80 del contenedor al puerto 8080 de nuestro host.
